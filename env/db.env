# PostgreSQL superuser — must match DATABASE_USERNAME/PASSWORD in backend.env
POSTGRES_USER=openstream
POSTGRES_PASSWORD=changeme

# Keycloak database user — created by scripts/initdb.sh on first run
# Must match KC_DB_USERNAME/PASSWORD in keycloak.env
KC_DB_USERNAME=openstream_keycloak
KC_DB_PASSWORD=changeme_kc
