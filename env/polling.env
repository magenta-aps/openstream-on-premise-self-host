NODE_ENV=production
PORT=3000

# PostgreSQL connection — must match DATABASE_* in backend.env
PGHOST=openstream-db
PGPORT=5432
PGDATABASE=openstream
PGUSER=openstream
PGPASSWORD=changeme

DJANGO_ACTIVE_CONTENT_URL=http://openstream:8000/api/active-content/
